auto-install-peers=true
strict-peer-dependencies=false
# React Native / Expo 툴체인(autolinking 등)은 pnpm 격리형 node_modules를 완전히
# 지원하지 않아, 최상위에서 expo-modules-autolinking 등을 못 찾는다. Expo 공식 권장대로
# flat node_modules로 설치한다(EAS 클라우드 빌드의 pnpm install에도 동일 적용).
node-linker=hoisted
